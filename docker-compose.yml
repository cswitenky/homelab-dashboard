services:
  homelab-dashboard:
    build: .
    container_name: homelab-dashboard
    ports:
      - "8080:80"
    volumes:
      # Mount your custom config.json here
      - ./config.json:/usr/share/nginx/html/config.json:ro
    restart: unless-stopped
